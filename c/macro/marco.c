#include <stdio.h>
#define PI 3.1415
#define circleArea(r) (PI*r*r)

int main() {
    // float radius, area;

    // printf("Enter the radius: ");
    // scanf("%f", &radius);
    // area = circleArea(radius);
    // printf("Area = %.2f", area);
    int a = 2147483647;
    a= a+1;
    printf("%d",a);

    return 0;
}